---
description: Guidelines for pushing changes to the remote repository
---
1. Summarize all staging and commits made during the session.
2. Provide the exact "git push" command that is intended to be run.
3. Explicitly ask the user for permission before executing the push.
4. Only run the "git push" command once the user has given their approval.
